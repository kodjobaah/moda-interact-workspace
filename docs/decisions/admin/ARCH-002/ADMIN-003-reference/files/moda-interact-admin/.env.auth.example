# Local development: auth/login bypassed by architecture.
DEPLOYMENT_ENVIRONMENT_NAME=development

# Required only when running with authentication enabled (test/production).
AUTH_URL=
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Existing database configuration.
DATABASE_URL=
