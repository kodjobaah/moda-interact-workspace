# Queue Events

Document stable BullMQ queue names, job names, payloads, retry behavior and
idempotency keys here.

Every contract should identify its producer, consumer, versioning strategy and
failure behavior.
